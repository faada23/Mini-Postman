<Window x:Class="Mini_Postman.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Mini Postman" Height="500" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="10"/>   
            <RowDefinition Height="*"/>   
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>   
                <ColumnDefinition Width="Auto"/> 
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" SelectedIndex="0" Width="80" Margin="0,0,5,0">
                <ComboBoxItem Content="GET"/>
                <ComboBoxItem Content="POST"/>
            </ComboBox>

            <TextBox Grid.Column="1" Text="https://jsonplaceholder.typicode.com/posts/1" 
                     VerticalContentAlignment="Center" Padding="5"/>

            <Button Grid.Column="2" Content="SEND" Width="80" Margin="5,0,0,0"/>
        </Grid>
        
        <TextBox Grid.Row="2" 
                 Text="{Binding ResponseText}" 
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="14"
                 Background="#F5F5F5"/>
    </Grid>
</Window>